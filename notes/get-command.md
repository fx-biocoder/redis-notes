### What does the `GET` command do?

The `GET` command retrieves the value stored at a specific key. If the key exists, it returns the stored string. Otherwise, it returns nil.

### What is the syntax for the `GET` command?

`GET [key]`

### Can I retrieve values stored in key names with spaces?

Yes, but you have enclose those key names in double quotes.
